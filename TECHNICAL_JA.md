# TECHNICAL（日本語）— 探索型圧縮 × 量子インスパイア × QAOA融合

本プロジェクトは量子優位性を主張しません。QAOA(p=1)の古典シミュレーションで
エネルギー地形 C(z) から確率分布 P(z) を生成し、上位候補を優先試行することで
探索効率を上げます。

主要式（対応）：
- H_C|z⟩ = C(z)|z⟩
- U_C(γ)=exp(-iγH_C), U_B(β)=exp(-iβΣX_j)
- |ψ⟩=U_B(β)U_C(γ)|s⟩
- P(z)=|⟨z|ψ⟩|²
